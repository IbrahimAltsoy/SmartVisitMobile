import React from "react";
import { View, Text } from "react-native";
import { styles } from "./Login.styles";

const LoginScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>SmartVisit Giriş</Text>
    </View>
  );
};

export default LoginScreen;
